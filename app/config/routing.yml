appDefault:
    resource: "@AppBundle/Controller/DefaultController.php"
    type:     annotation

appCircuit:
    resource: "@AppBundle/Controller/CircuitController.php"
    type:     annotation
    prefix : /{_locale}/
    requirements:
        _locale: es|en

appCarrera:
    resource: "@AppBundle/Controller/CarreraController.php"
    type:     annotation
    prefix : /{_locale}/
    requirements:
        _locale: es|en

appPosition:
    resource: "@AppBundle/Controller/PositionController.php"
    type:     annotation
    prefix : /{_locale}/
    requirements:
        _locale: es|en

fos_user:
    resource: "@FOSUserBundle/Resources/config/routing/all.xml"
    prefix : /{_locale}/
    requirements:
        _locale: es|en
